# requirements-oficina.txt
# ---------------------------------------------------------------
# Las versiones para la maquina de la RED INTERNA, que no llega a
# PyPI: Python 3.10 (gabdejo/py_versions/3106) y los wheels de
# gabdejo/pypro_packs.
#
# NO confundir con requirements.lock.txt, que describe la maquina de
# desarrollo (Python 3.14, pandas 3.x): esos wheels no existen en el
# wheelhouse y alli no hay de donde bajarlos.
#
# Se instala sin red, desde carpetas de wheels:
#   scripts\"Instalar entorno.bat"
# o a mano:
#   .venv\Scripts\python.exe -m pip install --no-index ^
#       --find-links <pypro_packs> --find-links <dependencias-faltantes> ^
#       -r requirements-oficina.txt
#
# Solo se fijan las dependencias DIRECTAS; pip resuelve el resto con
# lo que haya en esas carpetas. Verificado contra el contenido de
# pypro_packs el 2026-09-14.
# ---------------------------------------------------------------

# --- En gabdejo/pypro_packs ---
numpy==2.2.6
pandas==2.3.3
psycopg==3.3.4
psycopg-binary==3.3.4
fastapi==0.137.1
uvicorn==0.49.0
python-dotenv==1.2.2
PyYAML==6.0.3
beautifulsoup4==4.15.0
lxml==6.1.1
openpyxl==3.1.5
requests==2.34.2
exchange_calendars==4.13.2
pyodbc==5.3.0
selenium==4.44.0

# --- NO estan en pypro_packs: vienen de la release deps-py310 ---
# https://github.com/RicardoSachs/pap/releases/tag/deps-py310
# python-multipart es imprescindible: sin el la API no arranca.
python-multipart==0.0.32
playwright==1.62.0
